# 決定と保留

最終更新: 2026-07-19

このファイルは「**なぜそうしたか**」と「**何が未決か**」を残す。
何をしたかは git 履歴にある。作業を再開したら `CLAUDE.md` の次にこれを読む。

---

## 運用環境・前提

**ここが変わると以下の判断の多くが覆る。**

- **このリポジトリは public。** GitHub 上で `tonkura-blog` は public 設定。
  勉強会の参加者は実名で Zoom に参加しており、症例検討では患者の年齢・職業・地域・
  施設名が語られる。**文字起こし本体をコミットしない**、`quote` は問いの成立に必要な
  最小限にとどめる、という制約はすべてここから来ている。
- **高次脳機能部の勉強会**は月1回・2時間・Zoom。主催は本人。第14回が 2026-07-10。
  次回は 2026-08-07 の予定。
- **文字起こしは Zoom AI Companion が生成する。** クラウド録画は行っていない
  （`has_recording: false`）が、transcript と summary は残る。`recordings_list` は
  0件を返すので、**録画がないことと文字起こしがないことは別**。
- **Zoom コネクタは有料アカウントに接続されている必要がある。**
  個人の無料アカウントに繋がっていると、勉強会は1件も見えず `us05web` ドメインになる。
  正しく繋がっていれば `us06web` で、主催した会に `meeting_roles: ["host"]` が付く。
- **テストが実質ない。** `__tests__/PhotoGallery.test.tsx` の2件のみ。
  既存の型エラーも1件ある（同ファイル内、変更前から存在）。
  **回帰を検出する手段がないので、依存の更新は慎重に。**
- **既存の高次脳機能データベースが 51本ある**（`content/database/higher-brain-function/`）。
  事典的な記述はそちらの担当で、wiki は未解決の問いを扱う。役割が違う。

---

## 決定済み

### wiki の構造

**`/wiki` に置き、既存パスの下に入れなかった。**
`content/database/higher-brain-function/` と扱うものが違う（事典 vs 問い）ため、
階層を混ぜると性質の違いが見えなくなる。相互参照は frontmatter の `refs` で行う。

**`related` は問い同士専用にし、事典への参照は `refs` に分けた。**
同じ配列に混ぜると型が曖昧になり、逆リンク生成で問いと記事を区別できなくなる。

**`id` は `q-001` からの連番。領域プレフィックスを付けなかった。**
問いは領域をまたぐことが多く（`q-004` は失語・病識・メタ認知にまたがる）、
id に意味を持たせると後から分類が変わったときに破綻する。分類は `domain` が担う。

**`q-000` はテンプレート見本。一覧・件数・議題キューから除外している**
（`lib/questions.ts` の `TEMPLATE_ID`）。実際の問いではないので未解決件数に混ぜると
議題が水増しされる。ただし完全に隠すとスキーマ変更時の型崩れに気づけないので、
一覧末尾からのみ辿れるようにした。**削除しないこと。**

**`_inbox/` にあること自体が「未採用」を表す。`status` に `inbox` を作らなかった。**
`status` は `open | mapped | evidenced` の3値。ここに4つ目を足すと wiki 側の
状態遷移が濁る。場所が状態を表す設計にした。

**スタブに `id` を振らない。採用時に採番する。**
未採用のまま番号を消費すると、捨てた問いの分だけ `q-NNN` が飛ぶ。

### lastmod の自動更新 — 一度失敗して設計を変えた

**最終的な方式:** `git status` で変更が検出されたファイルにだけ今日を打刻する。

**当初やろうとして破棄した方式:** git の最終コミット日を全ファイルに引き当てる。
**冪等にならなかった。** 実測で差分が 320 → 329 → 337 件と増え続けた。

原因は2つあり、**2つ目が致命的だった**。

1. `git log --name-only` は既定（`core.quotePath=true`）で非ASCIIパスを
   `\344\270\241...` と8進エスケープする。**全1323パス中1152（87%）が照合不能**だった。
   `content/neurology/` はほぼ日本語ファイル名なのでほぼ全滅。
   → `-c core.quotePath=false` で解決（**この知見は他のスクリプトでも有効**）
2. エスケープを直しても churn が残った。単一パスのログ解析という設計自体が
   不安定だったため、**設計を捨てた**。

**教訓として残す:** 全ファイルの日付を履歴から再構成する必要はなかった。
変更されたファイルだけを対象にすれば構造的に冪等になる。

### 依存の更新

**`next` 16.1.1 → 16.2.10、`@next/*` 15.1.7 → 16.2.10。**
メジャーがずれていた。現状 `.mdx` は `gray-matter` + `marked` で処理していて
`@next/mdx` を経由しないため実害は出ていなかったが、MDX 内で JSX を使う段階で問題になる。

**`@mui/material` `@emotion/react` `@emotion/styled` を削除した。**
全ソースを検索して**参照が1件もなかった**（node_modules 26MB 相当）。
6.4.8 → 9.2.0 と3メジャー差があり、使っていないものを追従させる理由がない。

**据え置いたもの（意図的）:**
- `@niivue/niivue` 0.52 → 0.69 — **1.0未満で破壊的変更が入りやすく、脳ビューア3ファイルが
  依存している。** 回帰を検出するテストがないので単独で検証してから
- `eslint` 9 → 10、`@types/node` 22 → 26、`@types/jest` 29 → 30 — メジャー差

### 調査の方法論（Phase 2 で確立）

**詳細は `CLAUDE.md` に規約として書いた。ここには「なぜ」を残す。**

**PubMed に自然言語クエリを投げない。** PubMed は入力を AND 分解する。
`language is primarily a tool for communication rather than thought` と投げたところ、
看護用語の標準化・難民健康スクリーニング・医学教育のAI・救急待ち時間アプリ・
GPT-4o翻訳が返り、**目的の論文は1件も含まれなかった**。フィールドタグ付き
（`Fedorenko E[Author] AND Nature[Journal]`）に切り替えたら即座に見つかった。

**PubMed を既定の情報源にしない。** 生物医学限定で哲学・社会科学を含まない。
`q-005` `q-007` `q-008` の中核は哲学側にあり、SEP / IEP / PhilPapers が主情報源になる。
`q-008` に PubMed を投げてもほぼ何も出ない。**PhilPapers は 403 で到達できなかった。**

**`article_type` を必ず本文に出す。** Review と原著と症例報告を同じ顔で並べない。
実例として、会で「fMRIで二重乖離が示された」と紹介された Fedorenko 2024
（PMID 38898296）は **Review** であり一次研究ではなかった。一次研究は会の後に
PNAS で出ている（Kean et al. 2026, PMID 42406951）。

**検証パスは機械的に行う。** 最終稿の全 PMID をメタデータ照会で再取得し、
タイトル・n数・`article_type` の一致を確認する。「たぶん実在する」で通さない。

### 並列化の設計

**問いごとに subagent を分け、中央で検証する。**
`q-005` を1件だけパイロットして品質を確認してから、残り7件を並列展開した。
いきなり8件走らせていない。

**subagent の報告を信用しない。** 全208件の PMID を独立に照会して検証した。
検証自体も専任エージェントに委ねたが、**監査用に6件のタイトルを原文で報告させ、
自分の記録と照合した**（「検証者を誰が検証するのか」を空白にしないため）。

**結果:** 捏造ゼロ。指摘は `article_type` の副次タグ脱落7件のみで、
うち2件は**同じ論文を `q-001` と `q-002` で違う書き方をしていた内部不整合**だった。
並列化で起きうる典型的な劣化なので、次回も同じ検査を行うこと。

### q-010 を切り出した理由

`q-007`（「内言語」は何を指すか）の調査前に、**日本語の「内言語」と英語の
*inner speech* が同じものを指すのかを確かめる必要があった**。
結果、指すものが違った。心理学・神経科学系の辞典は「言語以前の思考や概念」まで含めるが、
失語症学系は「言語心像を思い浮かべる機能」という狭い対象を指す。
**用語が乱立しているのではなく、同一の語が分野をまたいで別の対象を指していた。**

---

## 未決事項

### 🔴 `q-003` の `evidenced` 判定

3条件（前提の明示・証拠の格付け・反証パス通過）は満たしているが、
**結論は「速い遅いは一定しない」**。`evidenced`（証拠あり）というラベルが
「答えが出た」と読まれないか。

**決めるのは本人。** 選択肢は (a) `open` に戻す、(b) ラベルの定義を
`CLAUDE.md` で明確にする、(c) このままとする。

### 🔴 `q-004` を分割するか

三層（自己モニタリング／病識／メタ認知）の分解は**部分的に支持された**が、
**Banks & Weintraub 2008（PMID 18194832, n=55）が層2と層3に「注目すべき差なし」と報告**
しており、最大の反証になっている。文献側も三分割を出すが切り口が違う
（CAM の三分類、Michel 2024 の三段階）。

判断材料は `q-004` の未解決点に賛否両論で整理済み。**決定はしていない。決めるのは本人。**

### ゲート2（merge）

`feat/wiki-phase1` が `main` から11コミット先行。**Vercel プレビューでの確認が未了。**
引き継ぎ計画では「プレビューで描画後の wiki を確認 → merge」がゲート2にあたる。

なおブランチは `feat/wiki-phase0` の上に積んである（Phase 0 を独立に merge できるように）。

### 粒度

1問いあたり 400〜600行になった。読み物として重いかもしれない。
分割するか、要約を冒頭に置くか、このままとするか。

### 日本語圏の文献

**ほぼ全問で未調査。** PubMed と SEP が英語圏中心のため構造的な偏りが残っている。
本人が教科書の自炊 OCR を用意する意向を示している。特に効くのは:

- `q-010` — 日本高次脳機能学会の公式用語集（**非公開で参照できなかった**）、
  失語症学の標準教科書での「内言語」の定義
- `q-008` — 日本の行政的な高次脳機能障害の診断基準、日本語圏の障害学

### `q-008` のマクルーハン

会で言及された論者だが**完全に未調査**。SEP にも IEP にも項目がない。
メディア論側の主張は一切書いていない。次回再訪の最大の欠落。

### `.mcp.json` のコミット

引き継ぎ計画では Zoom と PubMed を `--scope project` で追加して
`.mcp.json` をコミットするとされていたが、**保留した。**
これらは claude.ai のコネクタで、リポジトリの設定ファイルで再現できるものではない。
動かない設定を固定しても再現性の役に立たない。

### Phase 3（依存グラフの可視化）

引き継ぎ計画では「問いが20〜30本たまり構造が見えてから」。
現在10本なので、**会を2〜3回分ためてから**が妥当。

---

## 検証状況

### 確認したこと

- **ビルド通過**（`next build`、Turbopack）
- **11ページすべて HTTP 200**（`/wiki/q-000` 〜 `/wiki/q-010`、`next start` で実測）
- **`refs` の遷移先7件すべて実在**（既存の高次脳機能データベースへのリンク）
- **全208件の PMID を実在確認**。存在しない PMID・別論文・著者違い・雑誌違い・
  発行年違い・n数の食い違いはいずれも0件
- **`lastmod` の冪等性**を3シナリオで実測（クリーンな状態で5回実行 → 差分0のまま／
  日本語名ファイルを編集 → 1回打刻して以降増えない／新規未追跡ファイル → 正しく打刻）
- **逆リンク生成**（`q-007` ← `q-004` `q-005` `q-006` `q-009` `q-010`）
- **`_inbox` の内容が wiki に漏れない**こと
- **個人情報・参加者実名の混入なし**（全 `.mdx` を grep）
- `jest` 通過（ただし2件のみ）

### 確認していないこと

- **Vercel プレビューでの描画。** `curl` で HTML を取得して検証しただけで、
  **ブラウザで実際に見ていない。** レイアウト崩れや `prose` の効き方は未確認
- **日本語文献の内容**（上記「未決事項」参照）
- `q-003` が引用している **Research Square のプレプリント**（PMID 41646292）の中身。
  プレプリントである旨は明記済みで結論の根拠には使っていない
- **PDF からのテキスト抽出ができない。** `poppler-utils` が入っていないため、
  神経治療学の総説（`q-010` で参照したかったもの）を読めなかった。
  **システムへの変更なのでインストールしていない**
- `@niivue` 系の脳ビューアが依存更新後も動くか（回帰テストがない）

---

## 照会待ち

- [ ] 教科書の自炊 OCR（`q-010` `q-008` の日本語文献のため）
- [ ] `poppler-utils` を入れてよいか（PDF 抽出。システム変更のため未実施）
